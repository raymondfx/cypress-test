/**
 * A basic HTTP header.
 * @example
 *   { "Authorization": "Bearer xyz" }
 *   { "Content-Type": "application/json" }
 */
export interface HTTPHeader {
    [key: string]: string;
}
export interface APICredentialsOptions {
}
export declare abstract class APICredentials<O extends APICredentialsOptions> {
    abstract getAuthenticationHeader(options?: O): Promise<HTTPHeader>;
}
export declare class BasicAuthCredentials extends APICredentials<APICredentialsOptions> {
    private readonly username;
    private readonly password;
    constructor(username: string, password: string);
    getAuthenticationHeader(): Promise<HTTPHeader>;
}
export declare class PATCredentials extends APICredentials<APICredentialsOptions> {
    private readonly token;
    constructor(token: string);
    getAuthenticationHeader(): Promise<HTTPHeader>;
}
export interface JWTCredentialsOptions extends APICredentialsOptions {
    authenticationURL: string;
}
export declare class JWTCredentials extends APICredentials<JWTCredentialsOptions> {
    private readonly clientId;
    private readonly clientSecret;
    private token?;
    constructor(clientId: string, clientSecret: string);
    private getToken;
    getAuthenticationHeader(options: JWTCredentialsOptions): Promise<HTTPHeader>;
}
