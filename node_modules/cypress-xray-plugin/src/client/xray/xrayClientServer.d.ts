/// <reference types="cypress" />
import { BasicAuthCredentials, PATCredentials } from "../../authentication/credentials";
import { ExportCucumberTestsResponse, ImportCucumberTestsResponse } from "../../types/xray/responses";
import { XrayClient } from "./xrayClient";
export declare class XrayClientServer extends XrayClient<BasicAuthCredentials | PATCredentials> {
    private readonly apiBaseURL;
    /**
     * Construct a new Xray Server client.
     *
     * @param apiBaseURL the Xray server base endpoint
     * @param credentials the credentials to use during authentication
     */
    constructor(apiBaseURL: string, credentials: BasicAuthCredentials | PATCredentials);
    protected dispatchImportTestExecutionResultsRequest(results: CypressCommandLine.CypressRunResult): Promise<string | null>;
    protected dispatchExportCucumberTestsRequest(keys?: string, filter?: number): Promise<ExportCucumberTestsResponse>;
    protected dispatchImportCucumberTestsRequest(file: string, projectKey?: string): Promise<ImportCucumberTestsResponse>;
}
