/// <reference types="cypress" />
import { JWTCredentials } from "../../authentication/credentials";
import { ExportCucumberTestsResponse, ImportCucumberTestsResponse } from "../../types/xray/responses";
import { XrayClient } from "./xrayClient";
export declare class XrayClientCloud extends XrayClient<JWTCredentials> {
    /**
     * The URL of Xray's Cloud API.
     * Note: API v1 would also work, but let's stick to the more recent one.
     */
    private static readonly URL;
    protected dispatchImportTestExecutionResultsRequest(results: CypressCommandLine.CypressRunResult): Promise<string | null>;
    protected dispatchExportCucumberTestsRequest(keys?: string, filter?: number): Promise<ExportCucumberTestsResponse>;
    protected dispatchImportCucumberTestsRequest(file: string, projectKey?: string, projectId?: string, source?: string): Promise<ImportCucumberTestsResponse>;
}
